# 项目进度

更新时间：2026-08-28

## 已完成

- 创建Python 3.11+项目与隔离虚拟环境。
- 实现SQLite WAL数据库及世界、运行状态、地点、人物、关系、客观事件和人物记忆表。
- 实现世界快照、结构化动作、动作结果和轮次结果领域模型。
- 实现规则决策器与可替换 `DecisionProvider` 协议。
- 实现休息、进食、工作、旅行、交流、空闲六类动作及最终规则门禁。
- 实现状态修订检测、锁外决策、事务内结算和规则降级。
- 实现FastAPI、CLI与独立自动推进worker。
- 创建并实际推进“初始世界”，验证中文人物、地点、资源、事件和记忆持久化。
- 配置Git忽略的本地DeepSeek密钥文件，模型配置不会进入版本控制。
- 实现DeepSeek批量结构化决策、超时、有限重试、严格解析和规则降级。
- 实时查询模型列表，确认 `deepseek-v4-flash-vision-exp` 当前对该账号可用。
- 使用真实DeepSeek模型完成一轮世界推进，3项模型行动均通过本地规则并落库。
- 实现世界历史日志：中文Markdown、JSONL与逐轮JSON三种格式。
- 历史日志支持跨进程安全写入、自动同步、失败隔离和旧数据库回填。
- 已从SQLite回填“初始世界”前6轮、24条客观事件，并验证第7轮自动写入。
- 建立 `docs/design/` 设计文档集，记录分钟心跳、可调时间比例、模型裁判周期、事件分级、人物知识图、特质状态分离和小背包设计。
- 建立设计决策状态表，明确区分已实现、已确认待实现、提议和待决定。
- 实现默认1:1世界时钟、现实一分钟心跳、运行时加速/减速/暂停和离线不补算。
- 实现固定00:00与12:00自主裁判点，跨多个周期合并为一次裁判。
- 将模型裁判与世界时间推进拆离，旧 `tick` 入口改为只强制裁判。
- 实现 `world_heartbeats`、`character_state_updates`、状态余量累计和独立人物状态日志。
- 实现 `adjudication_runs`，保存触发类型、模型提案、规则拒绝、最终事件和降级状态。
- 实现主视角即时局部裁判且不重置全局裁判计划。
- 真实数据库已从备份安全迁移，旧世界、人物和28条历史事件保持完整。
- 隔离世界真实DeepSeek定时裁判验证成功：08:00心跳到12:00，只裁判一次且未降级。
- 实现FastAPI同源轻量世界控制台，无Node常驻依赖。
- 控制台支持世界查看、人物状态、调速、心跳、全局裁判、主视角介入、事件与裁判记录。
- 完成1280像素桌面和390像素窄屏真实浏览器检查，无横向溢出或控制台错误。
- 实现前端实时世界时钟：worker在线时在分钟心跳之间平滑走时，worker离线时冻结权威时间并明确提示。
- 真实浏览器验证worker在线提示；临时1×运行时世界时间在2.2秒内连续变化，随后恢复原暂停状态，控制台无错误。
- 增加Windows一键启动与一键停止入口，支持环境检查、隐藏进程、健康等待、自动打开控制台和运行日志。
- 验证重复启动保持单API、单worker；停止后进程数为0、PID文件删除且备用端口释放。
- 将人物反向饥饿值安全迁移为0至100的正向饱食度；100最舒适，心跳降低饱食度，进食提高饱食度。
- 迁移保留旧客观事件和JSONL原文，真实数据库完整性及中文人物数据均已核验。
- 实现原子调速：旧比例结算、状态更新、比例修改和事件写入共用单个事务，一次调速只增加一次状态修订号。
- 重复设置相同时间比例返回无操作，不写事件、不增加状态修订号。
- 管理控制台将 `world.version` 明确显示为“状态修订号”。
- 建立 `docs/worldbuilding/` 正式世界设定集，记录表层剑与魔法、底层科幻和自然魔力场的核心方向。
- 固化星外前文明设施与人物认知边界：当代居民没有“科技”分类概念，只能记录未知物体的可观察现象。
- 固化无随机宝箱原则，将容器访问、实际持有、法律所有权、权利主张、遗迹授权和来源记录分离。
- 将魔法底层迁移为天然魔力场：地区魔力由能量密度、频谱、相干性与环境响应形成，过度施法会造成枯竭。
- 删除人体个人核心魔纹与个人储魔；法师通过导师训练神经共振，施法能量全部来自佩戴或携带的储能物品。
- 确认法师主要通过知识而非罕见血统完成激活，绝大多数人都具有潜在施法可能。
- 记录魔法知识垄断、平民起义、公共教育和由低魔社会向高魔社会演变的长期事件路径。
- 确认初次激活必须由掌握引导术的法师亲自完成；人类与传统精灵等社会主要由法师家族私有传承。
- 建立智慧种族设计框架，确认精灵、地精等种族存在，并将种族生理、文化和人物个人特质分开。
- 将区域种族改为具有历史来源的区域衍生族群，要求通过祖先分化、混血、迁徙适应或稳定改造形成可追溯谱系。
- 确认“地精”指哥布林式智慧种族而非侏儒，并排除天生邪恶或仅作为怪物存在的设定。
- 确认第一版种族树：人类、精灵、矮人、地精和兽人为主要基源种族，龙族和巨人为稀有基源种族。
- 确认灰精灵来自精灵分支，蜥蜴人来自人类与龙族形成的稳定混合谱系；具体形成过程待讨论。
- 开始人类种族卡，确认当前医疗、卫生、营养和社会风险条件下，普通人类常见实际寿命约为50年。
- 确认人类法师常见寿命约100至130年，少数强大且生活稳定者可达150至180年。
- 确认人类以环境适应和魔法知识兼容范围广为核心特点，但没有天然极端专精。
- 确认人类约15至17岁身体成熟、平均世代22至25年，并采用受环境和事件影响的前工业缓慢人口增长模型。
- 确认法师健康生育期只适度延长，不随百年寿命同比扩张。
- 开始精灵种族卡，确认其自然寿命约500年、成熟较慢、低生育和人口恢复缓慢。
- 确认精灵具有敏锐听觉和魔力感知，施法倾向高精度、高稳定、低损耗与长期维持。
- 确认普通精灵常见实际寿命约350至450年，精灵法师约700至900年，少数可达1000至1200年。
- 确认传统精灵法师家族由长寿引导师长期控制并实行严格知识分层，只在传承需要时少量吸收外人。
- 建立灰精灵分支：因灾害中的知识封锁与传统家族决裂，实行知识共有、引导义务和危险施法共同体监管。
- 确认灰精灵不建立法师学院，而以家庭副本、公共记录、临时研究组和法师亲自指导构成分散传承。
- 将精灵与灰精灵尚未确认的文化、地理、身体分化、灾害、跨种族引导、治理和隐藏起源集中登记为后续问题。
- 开始矮人种族卡，确认普通矮人自然寿命约100至150年，其余身体、魔法和社会提案仍待讨论。
- 确认矮人约17至20岁身体成熟、平均世代30至40年，常见实际寿命75至110年，法师寿命180至250年且少数可达300至350年。
- 确认成年矮人身高约1米，具有致密身体、低重心、高力量体重比、地下适应和受介质与噪声限制的震动感知。
- 确认矮人魔法倾向身体强化、材料感知、固化和长期稳定，并区分低耗结构改变与持续运行器物。
- 确认持续型器物会长期束缚魔力并消耗供能，制造、配额、拆解和旧器物回收共同影响地区魔力。
- 确认矮人人口缓慢增长，并将家庭、工坊、法师家族、工程共同体和地表贸易分开。
- 确认矮人围绕三维土地、共享设施、长期工程责任和魔力器物回收形成社会制度。
- 完成矮人第一阶段种族卡，并增加受材料上限、能量守恒和属性取舍约束的“物性调律”魔法。
- 开始地精种族卡，确认“狡诈”为观察弱点、隐藏意图、利用规则和非对称策略的统计倾向，而非天生邪恶或全知。
- 确认地精身高约0.7至0.9米、8至10岁身体成熟、自然寿命35至45年，法师寿命60至90年且少数可达100至120年。
- 确认地精安全输出较低但擅长低耗快速切换、多任务维持和有同步风险的群体施法。
- 修正魔法传承边界：法师亲自引导是共同规则，法师家族只是一种制度；开放种族可以建立学校、工坊、神殿、军事或共同体传承。
- 确认地精以多名法师共同维持的传习营地为主要传承方式，通过歌曲、鼓点、图案、实作和季节集会保存知识。
- 完成地精第一阶段种族卡：人口增长快但波动大，以扩展家庭、氏族、小型部落、临时联盟和习惯法组织社会。
- 开始兽人种族卡，确认身高1.8至2.1米、自然寿命65至85年、强体能与高食物空间需求。
- 确认兽人魔法倾向身体强化和短时爆发，并受营养、水分、组织承载、身体维护与长期寿命代价约束。
- 完成兽人第一阶段种族卡：人口受食物承载限制，可形成多种社会；家庭处理亲属，誓团组织生产互助，法师采用实践型导师传承。
- 重构龙族方向：龙族是大陆最早一批本土生命谱系之一，依靠漫长积累独立发展出极高科技，并受到AI不可干预其他国家的根级命令限制。
- 确认龙族内部采用包含生物改造、身体商品化和巨型企业联合体竞争的赛博朋克社会。
- 记录龙族角色对RAG认知隔离的新要求：科技知识与禁用术语必须按人物身份生效，不能与外部人物共享上下文。
- 收紧龙族对外边界：龙族对外仅为难以证实的传说，龙形不得暴露改装，人类拟态观察者只能记录而不能改变外部发展。
- 确认AI只在龙族自身面临存亡威胁时允许干预；其他国家、文明、种族或大陆毁灭不能触发援助。
- 确认人工魔法起源于人类模仿魔兽天然魔纹；当前外部文明只掌握完整单纹，进阶组合语法仅属于作者、前文明和龙族知识。
- 确认法杖固化“施法魔纹”，读取法师主动构建的几何神经活动并投影现实单纹；它不能读取普通思想或设计法术。
- 确认当前单纹是有方向和比例的二维闭合结构：最终几何决定效果，绘制顺序不改变功能，最后闭合只负责启动。
- 确认魔纹几何生成固定耦合频谱，并通过热、动量、电磁、材料或生命耦合产生效果。
- 确认自主设计魔纹不是龙族专属；任何文明达到样本、测量、数学、记录和实验条件后都能独立进入高魔阶段。
- 将法师长寿改由维护器物、治疗单纹和优越生活条件解释，激活本身不再自动延寿。
- 限制表层魔法基础设施：除灰精灵可能维护少量公共设施外，其他外部文明主要保留家族、工坊、部落或个人私有设施；极北龙族除外。
- 确认储能物品被动缓慢吸收环境魔力；汇聚储能纹可把周围场能导入刻蚀容器，但会造成局部消耗并受材料容量限制。
- 确认物质产生光时按能量守恒伴生魔力波；恒星是主要来源，持续光照和传播使贫魔区恢复，魔法照明无法形成永动。
- 确认法术距离和范围由完整单纹的已知细节变体调整；多数魔法被路径障碍阻挡，少数效果依耦合介质传播并衰减。
- 完成巨人第一阶段种族卡：身高3至5米、自然寿命250至350年、人口极少，以分散亲族和周期集会生活，魔法低频缓慢且范围大。
- 开始星球地理阶段，确认初始星球在稳定环双星轨道上围绕紧密双星运行，总光照适合复杂生命。
- 确认金白主星提供较强宽频魔力，橙红伴星提供较弱稳定低频魔力，双星周期改变充能频谱而不产生无限能量。
- 确认24小时日、20天双星周期、360天年、四个90天季节和约20至25度轴倾角。
- 确认一颗30天月相的大型月亮；双星与月相每60天形成共同大周期，一年共6次。
- 生成并保存整星球母图、北方主大陆、南方大陆、东方裂海群岛和极北封闭区五张自然地形地图；地图暂不包含国家、城市和科技设施。
- 确认前文明来自太空，并在星球轨道建立用于检测星球及文明状态的环状巨构。
- 确认轨道环状巨构的大部分模块已经损坏，只有主体与少量核心系统勉强运行；剩余检测能力、主体寿命和数据去向仍待讨论。
- 确认巨构损坏来自超长期持续运行：长期老化最终使自动修复机制本身失效，不采用战争或单次灾难摧毁的解释。
- 确认前文明完成轨道环状巨构后离开星球，不再使用“引领者”定位，也不向地表文明传输任何知识。
- 确认巨构可以被星球表面的生物观测；除龙族能够确认其外来科技性质外，其他现存种族只能使用本地天文、宗教和神话概念解释。
- 确认所有文明知识必须由本土种族自行观察、测量、研究和发现，巨构不得提供教程、任务或方向性答案。
- 确认巨构的地表表现为晴朗夜空中横贯天穹的暗银色弧带，不主动发光或传讯；各文明可观测和记录，只有龙族理解其科技性质。
- 确认天环可以影响本土文明的历法、航海、宗教和神话，但不同文化的解释可以互相矛盾，不能把文化记录当成巨构知识。
- 开始北方主大陆文明构建：建立中央河网、西部山脉、北部湖林、东部河口、南部高地五个区域骨架，以及粮食政权、工坊网络、海峡贸易圈、分散共同体和边地誓团等政治原型；正式国名、城市和边界仍待确定。
- 确认中央河网第一阶段采用“议约王国”政体原型：王权负责征税、道路、军队和外交，但主要水道、堤坝、渡口、粮仓与灌溉权受河流议约约束；现实参照仅提炼上游—下游矛盾机制，不复制现实国家历史。
- 确认议约王国建国事件骨架“漫堤与断流”：连续季节波动引发上游、下游和港口共同危机，临时河务会议与受议约约束的王室由此形成；持续年份、王室和首份议约仍待确认。
- 完成伊瑟拉三大陆第一版历史总稿：确定诺赫兰、塞拉菲尔、维斯塔裂海的名称、统一时代年表、区域历史、主要政体原型、跨区域贸易与当前矛盾；精确年代、城市数量、人物和条约细节留待后续修改。
- 完成三大陆国家、文化与冲突总册：登记诺赫兰5个、塞拉菲尔4个、维斯塔裂海4个主要政体；每个政体均包含形成历史、文化、魔法制度与当前冲突，并建立盐粮封锁、水源、航道税、知识流动和迁徙身份五类跨区域冲突。
- 完成作者层正典编年、现行权力与战争条约账本：确定 H 24816 年为当前年份、13个政体的建国年代和核心城市、公开权力代表与节期，并登记建国危机、三港航权战争、停火约及当前五条危机线。
- 完成本土谱系、灰精灵、蜥蜴人与龙族隔离史：明确雾幕热、灰幕放逐、副本议庭、苇湾接触、零涉协议与北阈四大企业结构，并将信仰与地方历法限制为可变的文化解释而非作者层全知真相。
- 完成极北封闭区作者层历史：明确霜阈城、衡生穹、远界港、玄铸环城，四大企业、人口范围、身体与人格规则、边界机制及其不能介入三大陆的内部冲突。
- 完成三大陆高对比政治地图：在保留自然地形的前提下标出13个主要政体的国界、行政区色块、核心城市、主要种族与图例，并提供可重复渲染脚本。
- 确认星球全部物种均为本土生命，星外前文明不是任何种族的祖先、播种者或改造者。
- 将龙族科技改为本土最古老生命谱系长期独立发展的成果，不再解释为保存或继承前文明科技。
- 实现本地Markdown世界设定RAG：中文BM25分块检索、稳定来源ID、上下文字符预算和无需模型调用的CLI检索检查。
- 将知识分为作者隐藏真相、叙事护栏与人物通用认知；未标注资料默认属于 `author_hidden`，叙事护栏每轮固定注入。
- DeepSeek人物决策在进入模型前排除 `author_hidden`，只注入 `guardrail` 与 `character_common`；输出泄露明确底层术语时拒绝本轮结果并交给规则引擎降级。
- 落地正式初始世界“伊瑟拉·澜誓城”：按开普勒自洽的 360 天年建立伊瑟拉历换算；17 地点、30 人物（24 政体代表 + 6 澜誓城本地）、6 核心激活；引擎裁判 limit=6 时只激活核心 6 人。
- 新增阿德伦当代常识（character_common），核心人物 traits/goals 可召回河流议约、水权、河务六席；新增 `world_engine/seeder.py` 与 CLI `seed-isera`。
- 前端控制台世界时间改用伊瑟拉历（展示“H 24816 年 1 月 1 日 08:00”），只改展示层 `app.js`，不改引擎时钟推进。
- 改用多元纪元法（朝代更替纪）：13 政权以近期危机为元年、以地标意象命名当代纪元（纪年数几百~千余）；控制台/CLI 主打主纪年“河誓纪 327 年”，环后纪年 H 保留作作者层参照，见 `28-multi-era-calendar.md`。
- 信息增强：人物卡姓名旁显示“身份”；事件详情展示“决定理由”+ 社交“对话台词”。决策器产出 `dialogue`（规则引擎给模板台词、DeepSeek 由模型生成），characters 表新增 `identity` 列（含旧库迁移），前端事件/人物卡同步丰富。
- 主视角感知限定：characters 新增 `is_pov` 列（玩家主控，玩家介入可切换）；DeepSeek 只为主控同地点人物生成对话，主控感知不到的社交 `dialogue` 置空（单测覆盖）。规则决策器对话改为按身份话题化的台词库。
- 玩家角色体系：新增唯一玩家角色 `is_player`+`is_pov`（`POST /api/worlds/{id}/player` 创建、`create_player_character`）；玩家自然语言意图经 `POST /api/worlds/{id}/player/act` 交给 AI(DeepSeek，失败降级规则)转成一次行动并结算进世界（写事件、版本+1、与心跳/裁判解耦）。前端玩家意图表单由“存草稿”改为真实提交。
- 玩家意图端到端：无玩家时提交意图被正确拒绝(400)；规则降级下“去东澜港”转成 TRAVEL 事件并落库。
- 扩充 world NPC：世界人物从 30 扩到 66，给 17 个地点各补 2-4 名普通居民(澜誓城 4、各城多2-4)，玩家到任何地点都立刻能看见周围人物且无空地点；玩家社交缺台词时引擎自动用规则补一句，保证玩家发起的对话内容始终可见。
- 社交对话改为双向：`ActionProposal` 新增 `reply`(对方回应)；规则/DeepSeek 均生成"自己说 + 对方回应"，玩家发起的对话事件完整显示双方发言(前端"你说:/对方:")。
- 完整战斗系统：`ActionType` 新增 `attack`；角色加 `health`(0-100)；攻击造成伤害并可被反击；对手 HP 归零=死亡(重要 NPC 记 `world.major_death`、普通记 `action.target_down`)，玩家 HP 归零=败北(原地苏醒记 `world.player_defeat`)；在 city/public 公开攻击引来守卫/河务介入(`world.warden_intervention`)。攻击意图(攻击/打/袭击/砍/杀)由规则与 DeepSeek 识别。
- 前端游戏化 + 物品/技能/关系：新增 `item_types`/`item_instances` 表与背包/装备位；角色带 `skills`；玩家初始"剑术+铁剑+疗伤药×2"，攻击读取武器/护符/技能加成；意图"捡/用/装备"→ `GATHER`/`USE`。`snapshot` 注入 `relationships`，前端按亲和/信任判友方/敌方并渲染战斗面板(敌我 HP/队友/技能/背包装备)、即时反馈浮层(`#action-result`)与位置地图(`#position-map`)。
- 前端体验优化：事件/裁判列表按 diff 更新(仅真正有新事件才重绘，避免每 5 秒重刷编年/滚动丢失)；连接状态在周期轮询时静默(不再反复"正在同步/重连")；编年者抽屉新增"主视角关系网"(以主视角为中心放射，敌对红/友好青、线粗=亲密度)。
- 世界自然运转(基础)：默认裁判间隔从 12 世界小时缩到 3 小时(可 `.env` 更短"很活")，正式世界已迁移；人物行为改由 `goals` 驱动——有目标者更主动(`_select_active_characters` 加权)、行为贴合自身长期目标(找/寻→打探、去/前往→赶路、工作/找活→干活、守/护→警戒)，而不是只看"谁饿谁动、随手做一件"。

## 2026-08-27 前端与世界坐标重构

- 按 `docs/frontend-redesign-spec.md` 重构为左侧旅人/编年者导航与右侧房间式主内容；场景成为默认舞台，地图、编年、关系网、笔记本和三类编年者管理各自切页。
- 技能、背包、装备与自然语言行动改为常驻浮动桌面；行动草稿按世界保留，提交后即时反馈并刷新编年。
- 增加人物档案弹层、近期回忆、交谈/攻击预填、编年分类筛选、以玩家为中心的 SVG 关系网与节点拖拽代码路径、浏览器本地笔记本。
- 世界位置事实改为经纬度：`locations.longitude/latitude`、`characters.longitude/latitude`；创建人物与旅行结算同步坐标，旧库稳定回填且不改写已有坐标。
- 增加 `world_maps` 与 `map_features`，统一登记世界母图、地图边界、建筑/地标/街区等未来空间要素；快照 API 直接返回可绘制地图、地点、人物和已知要素。
- 生成8张 `tiles/z1/` 大比例尺一级地图；它们以4列×2行连续覆盖 `[-180,180] × [-90,90]`，内部使用半开区间避免重复归属，可按行列拼回世界母图。
- 浏览器实测桌面/窄屏场景、地图、编年、关系网、人物档案、笔记本和世界流速切页；控制台无错误。关系节点拖拽代码已接入，但自动化拖拽手势未形成有效验证证据。
- 迁移前备份真实数据库到 `backups/spatial-redesign-20260827-2330/world.db` 并记录 SHA-256；迁移后真实库 `integrity_check=ok`、地点/人物缺失坐标均为0、地图9张、已知地图要素4个。

## 2026-08-28 连续地图移动与事件分层

- 地图取消手动切图，改为8张一级切片组成同一连续画布；滚轮以指针为中心在100%至800%间缩放，拖拽平移，重置与缩放按钮保留为无滚轮设备的辅助入口。
- 地图不再绘制任何 NPC 点位，只保留玩家、已知地点/建筑和当前移动目标；浏览器实测地图点位中 NPC 数量为0。
- 新增 `characters.movement_type`、`movement_speed_kmh`、`active_vehicle_id`，以及 `vehicles`、`character_movements`；支持陆行、飞行、轮船、钻地、水行等移动类型和载具速度。
- 地图点击与自然语言 `TRAVEL` 共用持续移动服务：创建行程时不改坐标，世界心跳按速度推进路程；途中可记录新人物相遇，抵达后才结算目标地点附近的地点归属，任意荒野坐标仍保留精确位置。
- 新增开始移动、取消移动、切换交通工具 API；全局移动状态条显示类型、速度、预计抵达、已走/总距离和取消按钮，交通工具浮动栏目支持徒步与人物拥有的载具。
- 地点交互、交流、攻击与附近人物判断改为经纬度距离，不再以相同 `location_id` 代替真实接近；移动中的 NPC 不参与新一轮自主裁判。
- `world_events.importance` 将事件分为 `major`/`routine`；世界编年只读取重大事件，新增“日志”页显示对话、普通战斗、移动、相遇、取消、抵达和内部操作。
- 真实库迁移前备份到 `backups/movement-redesign-20260828-005115/world.db`；迁移副本演练与真实库均为 `integrity_check=ok`，事件重要级无空值。
- 浏览器验证：滚轮缩放从100%到188%、拖拽平移均改变地图变换；隔离测试世界中点击地图成功创建持续行程，未瞬移，取消后状态消失且开始/取消均进入日志；交通栏目显示陆行与5 km/h，控制台无错误。
- 地图覆盖图例改为独立屏幕覆盖层：底图相机单独缩放，地点名称、玩家标记和移动目标不再进入缩放合成层，只按地图变换重算屏幕坐标；避免反向缩放造成文字纹理重采样与模糊，同时保持固定像素尺寸。
- 玩家地图标点改为 `requestAnimationFrame` 逐帧插值；移动时以5位小数更新经纬度，标点位置每帧按当前相机重新投影，时钟和进度文字限制为每200毫秒刷新以避免高频布局开销。隔离世界在连续300毫秒采样中标点横坐标 `245.115 → 245.227 → 245.341 px`，经度 `W 66.46011° → W 66.39772° → W 66.33629°`。

## 2026-08-28 候选语义地形、分层地点与NPC激活

- 生成北方主大陆第一版候选语义地形包：768×512高程、海陆、坡度、地表栅格，以及4条候选河流、3条候选道路、桥渡/港口/山口门户和高层路线图；生成器重复运行哈希变化为0。
- 候选地形统计：陆地约48.24%、外海约50.70%、内陆水体约1.05%、最高海拔4396米；所有数据保持 `candidate`，没有导入真实路线规划器。
- 地点增加归属半径、优先级和父级；人物增加实时可空 `current_location_id`。建筑/街区优先于城市，离开全部范围后正确成为无名地域，旧 `location_id`只保留兼容。
- 生成1600×1600澜誓城导航式详细地图：SVG运行资源与PNG预览、12条分级道路、165个独立建筑轮廓、4条河流、8类POI和桥渡；名称来自正典，几何为候选且可确定性重建。旧版海报式大方框示意图已被替换。
- 按用户建议调用内置图像生成接口生成无文字导航视觉底图，再由SVG叠加权威道路名称、POI、北向标和比例尺；生成图只负责视觉，坐标、区域、道路与设施数据仍由本地GeoJSON/SQLite约束。
- 地图记录绑定地点并区分 `world/tile/detail`；前端根据实时/预测坐标自动进入澜誓城详细图，离开范围后恢复8片世界地图。隔离浏览器验证城内/城外双向切换，控制台无错误。
- NPC增加激活状态、策略、理由、激活期限、距离半径和概率。30名正式/特殊NPC持续激活，36名普通居民为背景；背景NPC仍可观察但不进入自主裁判。
- 距离激活概率随距离按幂函数下降，超过半径为0；高关系NPC持续激活；主视角点名交谈可令背景NPC临时激活30个世界分钟。
- 迁移前备份 `backups/area-activation-redesign-20260828-175800/world.db`；副本演练与真实库均为 `integrity_check=ok`，真实玩家当前在城外，`current_location_id=None`，未被错误归回旧地点。

## 2026-08-29 Noryia 语义地形与真实路线移动

- 新增生成器 `scripts/generate_noryia_navigation_data.py`：以 `Noryia Full *.json` 为唯一地理输入，生成 `docs/worldbuilding/maps/navigation/noryia/` 的候选导航产物（`metadata.json`、`terrain.geojson`、`terrain-index.json`、`surface.geojson`、`waterways.geojson`、`roads.geojson`、`crossings.geojson`、`route-graph.json`、`movement-rules.json`、`audit-overlay.svg`），全部标 `candidate`，且记录源文件 `sha256`；重复生成产物字节一致（确定性）。
- 地形模型：`pack.cells` 为运行时基本单元，按 `cell.f` 指向的 `pack.features` 类型区分外海(2097)/内陆湖(42)/河流格(801)/陆地(2922)；高度码经单调映射转米并记入 `metadata.json`；坡度由邻接格高程差与球面距离计算；生物群系映射为受控地表类型。
- 数据库迁移：新增 `navigation_datasets` / `navigation_nodes` / `navigation_edges` 三表与索引，`character_movements` 增补 `route_json`、`route_index`、`route_distance_km`、`navigation_dataset_id`、`replan_reason`；旧库由 `_migrate_navigation_columns` 补列。
- 新增 `world_engine/navigation.py`：`TerrainService`（从产物采样任意经纬度地形上下文与门户，`from_memory` 支持合成地形做单测）与 `NavigationDatasetImporter`（导入 route-graph 并支持 `mark_approved`）。
- 新增 `world_engine/routing.py` 分层路线规划器：在格网上按移动类型做 A*（陆地不跨外海/大河需门户、船舶仅水体+港口、飞行可跨海、梯度>35°需山口），返回可验证折线、总距离、ETA、途经地形摘要、所需门户、不可达原因与数据集版本；禁止退回穿模直线。
- 改造 `MovementService`：`start` 在存在 `approved` 数据集时强制规划路线（不可达返回结构化原因），否则退回旧直线；`advance` 沿 `route_json` 折线逐段按有效速度推进，坐标不跳跃、累计距离不超总距离；新增 `_advance_one`/`_walk_route`。
- API：`POST /player/move` 返回 `{movement, route}`；新增 `GET /api/worlds/{world_id}/terrain` 只读地形采样；前端地图绘制移动路线折线、移动栏显示地表/速度/所需门户与数据集状态、悬停显示当前地形与海拔。
- 新增测试 `tests/test_noryia_navigation.py`（产物完整性、源哈希、快照计数、确定性、水陆分类）、`tests/test_routing.py`（外海/船舶/飞行、跨河需门户、坡度山口、道路提速）、`tests/test_movement_routing.py`（已审核数据集移动走路线、逐步推进、不可达原因、地形端点）。

## 2026-08-29 地图样式切换与地形图

- 生成器 `scripts/generate_noryia_navigation_data.py` 新增 `terrain-base.svg`（生物群系与海陆矢量底图）与 `elevation.svg`（相对高度色带），刷新产物，并在 `metadata.json` 的 `style_assets` 注明「相对高度、映射待审核」，不宣称精确海拔米；生成保持确定性。
- 数据库新增 `world_map_preferences` 表（`political/terrain/elevation/passability`，UI 偏好仅影响展示，不影响规则）。
- API 新增：`GET /worlds/{id}/map-styles`（可用样式 + 当前偏好 + 数据集状态 + 通行图可用性）、`PUT /worlds/{id}/map-styles`（保存偏好）、`GET /worlds/{id}/navigation-dataset`（审核通过/候选数据集元信息）、`GET /worlds/{id}/passability`（对给定范围做粗采样通行图，规则由后端决定）。`terrain` 端点扩展 `movement_type` 与 `passability` 通行摘要。
- 前端地图新增「样式」下拉框（政治图/地形图/高程图/通行图）；切换保留 `mapView.scale/x/y` 与坐标、路线与已选目的地。地形图/高程图切换对应 SVG 底图，通行图叠加后端可达性采样格，悬停读取真实格网地形；政治图维持现状。
- 新增测试 `tests/test_map_styles.py`（默认政治图与偏好持久化、数据集状态、导航数据集端点、terrain 通行摘要、通行图端点、地形采样与移动路线一致性）与 `test_noryia_navigation.py` 的样式资产校验。

## 2026-08-30 世界元素注册器第一阶段

- 新增统一 `WorldElementRegistry` 与 Pydantic 判别联合，支持人物后代、聚落、建筑、巨构/遗迹和动态世界观五类注册。
- 新增注册请求、效果、统一实体索引、血缘、建设项目、建筑、巨构和动态知识表；实际实体仍进入专用表。
- 注册请求强制引用已结算来源事件；人物申请者必须是事件参与者；世界内幂等键阻止重复应用。
- 处理器使用 SQLite SAVEPOINT：拒绝或故障不留下局部事实，失败请求仍保留审计。
- 玩家/NPC不能瞬间创建成熟城市/完工建筑，也不能把人物说法直接升级为作者正典。
- 新增提交、列表和详情 API；RegistrarAgent、自动行动识别、建设心跳和动态知识 RAG 合并留到后续阶段。
- 真实数据库迁移前备份到 `backups/element-registry-20260830-142649/world.db`，SHA-256 为 `84A8CF15997C5F422982DC0061AC26E380A5AA6DDF38B30B22FC7C75545F7375`；迁移前后世界、地点、人物和事件计数一致，`integrity_check=ok`。

## 2026-08-30 世界元素注册器第二阶段

- 玩家行动成功结算后，以来源事件为边界运行确定性 `RegistrationIntentDetector`；明确的建城、建筑、遗迹、家庭计划、人物观点和地方传闻会自动提交严格候选，失败不回滚已完成行动。
- `PlayerActionResult` 返回 `registration_ids`，自动注册成功时世界版本同时反映行动事务与注册事务。
- 新增建设项目列表和状态 API；项目进入 `constructing` 后按世界时间推进，完成时更新建筑/巨构或创建正式聚落，并记录建设完成事件。
- 动态 `knowledge_entries` 已并入 Agent 场景检索：私有观点只给作者本人，地方说法按来源地点，公开知识全局可见；作者正典仍不进入普通人物上下文。
- 真实数据库第二阶段迁移前备份到 `backups/element-registry-phase2-20260830-145437/world.db`，SHA-256 为 `EC379D96AD3CF490A4698ACDA8E85BAB9381101828F98C17F9E6DDFDFA820BFB`；迁移后 `integrity_check=ok`、外键错误为0、现有数据计数不变。

## 2026-08-30 世界元素注册器第三阶段

- 新增可选 RegistrarAgent：只在确定性识别未命中且行动包含长期影响关键词时调用 DeepSeek，输出最多3个严格候选；模型失败不影响已结算行动，也没有数据库写权限。
- 新增 `family_plans`：家庭计划保存父母/监护人、预定姓名和到期世界时间；当前统一等待270个世界日，出生必须引用到期计划并保持成员/姓名一致。
- 建设项目进入 `constructing` 时原子检查并扣除地点 `resources_json` 与申请者资金；不足时状态和资源都不变化，并保留明确错误。
- 前端编年者导航新增“元素注册”审计页，可查看注册状态、拒绝原因、效果、建设进度并启动合法项目。
- 使用项目脚本重启 API 与 worker；健康接口、注册列表、建设列表、OpenAPI 新路由和前端入口均已在 `127.0.0.1:8000` 验证。
- 真实数据库迁移前备份到 `backups/element-registry-phase3-20260830-150807/world.db`，SHA-256 为 `649EB622F9D7025888EC028BD51779901010E8D2618BDD8E48308F26AE478DE5`；迁移后完整性正常、外键错误为0、真实注册记录仍为0。

## 2026-08-30 世界元素注册器第四阶段

- 自动检测出的玩家长期影响改为 `proposed`；注册审计页提供确认与拒绝按钮，确认后才应用事实表副作用。
- 新增 `species_profiles` 和人物 `species`/`birth_world_time`；种族档案控制孕育天数、成年年龄与兼容性，旧人物按已成年兼容处理。
- 新增 `land_claims` 与 `construction_escrow`；聚落规划预留地块，开工扣除资源/资金，取消按未完成比例退款并写 `world.construction_cancelled` 补偿事件。
- 真实数据库迁移前备份到 `backups/element-registry-phase4-20260830-195405/world.db`，SHA-256 为 `98FBAAB9532EBB4649E970C053997ED0996AFCB8B9CF3D7DBBA01EBDE4A08767`；迁移后完整性正常、外键错误为0、现有数据计数不变。

## 当前验证

- `pytest`：137项测试通过、2项环境相关测试跳过；`ruff check world_engine scripts tests` 与前端 `node --check` 通过；另有1条Starlette上游TestClient弃用提醒，不影响当前功能。
- CLI `seed-isera` 创建正式世界成功：17地点、30人物、6核心，world_time=2040-04-01 08:00（对应 H 24816 年 1 月 1 日）。
- `knowledge-search "河流议约 调水权 河务六席 大粮仓" --audience character_common` 命中阿德伦常识块。
- 正式世界裁判验证（rules、limit=6）：选中人物恰为核心 6 人、背景 NPC 未被选中；heartbeat 推进 60 秒、人物状态正常、无报错（预留 accumulator 行正确）。
- `to_era` 纪元换算单测通过：主纪年“河誓纪 327 年”、＋360 天 → 328 年、13 政权纪元齐全；CLI `show` 输出 `current_era=河誓纪 327 年 …`。
- `ruff check .`：全部通过。
- `compileall`：通过。
- `pip check`：通过。
- 世界设定RAG：实际加载392个知识块；人物安全检索保持当前时代常识边界，作者层可检索天文、环后编年、三大陆国家文化、战争条约、种族谱系、灰精灵、龙族隔离、宗教记忆与政治地图说明。
- 真实HTTP：健康、世界列表、推进与事件接口均为200。
- SQLite `PRAGMA integrity_check`：`ok`。
- 独立worker：真实推进成功。
- “初始世界”可正常读取中文地点、人物及正向 `satiety` 字段。
- 真实数据库调速验证：1×切换至2×仅增加一次状态修订，重复设置2×不变化，恢复1×再增加一次；验证过程未推进世界时间。
- 旧 `hunger=63` 的人物迁移后 `satiety=37`，符合 `100 - hunger` 转换规则。
- “初始世界”已验证1分钟心跳，世界时间由15:00推进到15:01，未调用模型。
- 敏感信息扫描未发现API密钥、密码、Secret或私钥。

## 尚未开始

- 三大陆主要国家、历史、文化与冲突第一版设定已经完成；正式初始世界“伊瑟拉·澜誓城”已落地（全新 world，未替换测试世界）。精确国家边界、次级城市、人物家系、条约原文与更多初始人物细节仍待继续讨论。
- `docs/design/` 中的事件分层、知识图、特质表和库存表仍是目标设计，尚未实施数据库迁移。
- 复杂谈判、谎言和组织摩擦所需的第二阶段模型冲突裁判尚未实现；当前使用本地规则顺序校验。
- 控制台尚未实现登录和管理员权限，不能直接暴露到公网。
- 环境、经济、长期目标、谣言和记忆压缩。
- 模型调用用量、成本上限与运营可观测性。
- 表现形式选择与对应前端。
- Linux服务器systemd、Caddy、备份和SSH部署。
